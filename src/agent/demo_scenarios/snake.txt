> create a snake game in pygame

# I'll build a small, playable Snake game with pygame.

$ python -m venv .venv && . .venv/bin/activate
$ pip install pygame

# Writing snake.py

+ import pygame, random, sys
+ pygame.init()
+ W, H, CELL = 480, 480, 24
+ screen = pygame.display.set_mode((W, H))
+ clock = pygame.time.Clock()
+
+ snake = [(5, 5)]
+ direction = (1, 0)
+ food = (10, 10)
+ score = 0
+ # ...game loop: input, move, grow, collide...

# Running it to verify the game launches.

$ python snake.py
• window opens: 20x20 grid, snake moving right
• arrow keys steer; eating food grows the snake
• self-collision ends the game; score shown in the title

# Done — snake.py is a complete, playable Snake game.
