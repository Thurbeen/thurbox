{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "thurbox-cli session signal --state idle >/dev/null 2>&1 || true  # managed by thurbox `extension install` — do not edit"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "thurbox-cli session signal --state working >/dev/null 2>&1 || true  # managed by thurbox `extension install` — do not edit"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "thurbox-cli session signal --state working >/dev/null 2>&1 || true  # managed by thurbox `extension install` — do not edit"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "case \"$(cat | sed -n 's/.*\"message\":\"\\([^\"]*\\)\".*/\\1/p')\" in *permission*|*Permission*|*approval*|*approve*) thurbox-cli session signal --state blocked >/dev/null 2>&1 ;; esac; true  # managed by thurbox `extension install` — do not edit"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "thurbox-cli session signal --state done >/dev/null 2>&1 || true  # managed by thurbox `extension install` — do not edit"
          }
        ]
      }
    ]
  }
}
