name: Deploy Website

on:
  push:
    branches: [main]
    paths:
      - 'website/**'
      # Demo media is copied into website/assets/ at deploy time, so a change to
      # the rendered videos must also redeploy the site (e.g. after re-running
      # scripts/demo/record.sh). Without this the live site keeps stale mp4s.
      - 'docs/media/**'
      # The build reads these from the repo root, outside `website/`:
      # `eleventy.config.js` generates the CSS bundle, the TOC rail and the code
      # highlighting, and the two package files decide which versions do it. A
      # Renovate bump to `node-html-parser` or `prismjs` changes deployed HTML
      # with nothing under `website/` touched.
      - 'eleventy.config.js'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 26
          cache: npm

      # Demo videos are copied into the Eleventy input (website/assets/) before the
      # build, so the passthrough-copy step picks them up into the built _site/.
      - name: Copy demo videos into website assets
        run: cp docs/media/*.mp4 website/assets/

      - name: Install dependencies
        run: npm ci

      - name: Build site with Eleventy
        run: npm run build:website

      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: _site

      - id: deploy
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
