(
    lints: [
        Module((
            name: "ui_no_claude_imports",
            matches: Module(".*::ui::.*"),
            rules: [
                RestrictImports(
                    allowed_only: None,
                    denied: Some([".*::claude::.*"]),
                    severity: Error,
                ),
            ],
        )),
        Module((
            name: "git_no_ui_imports",
            matches: Module(".*::git::.*"),
            rules: [
                RestrictImports(
                    allowed_only: None,
                    denied: Some([".*::ui::.*"]),
                    severity: Error,
                ),
            ],
        )),
        Module((
            name: "claude_no_ui_or_git_imports",
            matches: Module(".*::claude::.*"),
            rules: [
                RestrictImports(
                    allowed_only: None,
                    denied: Some([".*::ui::.*", ".*::git::.*"]),
                    severity: Error,
                ),
            ],
        )),
        Module((
            name: "project_no_local_imports",
            matches: Module(".*::project::.*"),
            rules: [
                RestrictImports(
                    allowed_only: None,
                    denied: Some([".*::claude::.*", ".*::ui::.*", ".*::git::.*", ".*::app::.*"]),
                    severity: Error,
                ),
            ],
        )),
    ],
)
