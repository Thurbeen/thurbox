# thurbox's interface

The guidance for this directory is in [`AGENTS.md`](AGENTS.md). Read that.

<!-- A pointer rather than a copy: two files saying the same thing drift, and the
     stale one is the one somebody reads. thurbox launches whichever coding CLI you
     configured, so the guidance is surfaced under each one's conventional name.
     The `flow` extension does the same with symlinks, which it can because it
     copies its own files; delivery here writes contents. -->
